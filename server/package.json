{
  "name": "server",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Backend API server for anime rating search",
  "main": "index.ts",
  "scripts": {
    "dev": "tsx --watch index.ts",
    "start": "tsx index.ts",
    "build": "tsc -p tsconfig.json",
    "type-check": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "got": "^14.4.7"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.5",
    "tsx": "^4.20.6"
  }
}
